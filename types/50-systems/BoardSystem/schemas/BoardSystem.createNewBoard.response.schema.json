{
    "$comment": "[🐸]",
    "$id": "https://schema.collboard.com/boardSystem/createNewBoard/response",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "Information about newly created board",
    "type": "object",
    "allOf": [
        {
            "$ref": "https://schema.collboard.com/response",
            "$comment": "Note: [🍋] This is a way how to extend the request schema; if there is a better or more elegant way, do it!"
        }
    ],
    "properties": {
        "systemName": {
            "const": "boardSystem",
            "$comment": "TODO: [🍃] Test that this is a valid system name BY THE FILENAME IN generate-message-api"
        },
        "actionName": {
            "const": "createNewBoard",
            "$comment": "TODO: [🍃] Test that this is a valid method name BY THE FILENAME IN generate-message-api"
        },
        "viewLink": {
            "description": "View link for the newly created board",
            "$ref": "#/$defs/url"
        },
        "editLink": {
            "description": "Editation link for the newly created board",
            "$ref": "#/$defs/url"
        }
    },
    "required": ["systemName", "actionName", "editLink", "viewLink"],
    "unevaluatedProperties": false,
    "$defs": {
        "url": {
            "type": "string",
            "pattern": "^https://[^/\r\n]+"
        }
    }
}
