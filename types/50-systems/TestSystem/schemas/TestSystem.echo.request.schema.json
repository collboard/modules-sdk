{
    "$comment": "[🐸] TODO: [🍃] Test that $id is in correct format BY THE FILENAME IN generate-message-api ",
    "$id": "https://schema.collboard.com/testSystem/echo/request",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "Command to echo given message",
    "type": "object",
    "allOf": [
        {
            "$ref": "https://schema.collboard.com/request",
            "$comment": "Note: [🍋] This is a way how to extend the request schema; if there is a better or more elegant way, do it!"
        }
    ],
    "properties": {
        "systemName": {
            "const": "testSystem",
            "$comment": "TODO: [🍃] Test that this is a valid system name BY THE FILENAME IN generate-message-api"
        },
        "actionName": {
            "const": "echo",
            "$comment": "TODO: [🍃] Test that this is a valid method name BY THE FILENAME IN generate-message-api"
        },
        "message": {
            "description": "Any message to echo back",
            "anyOf": [{ "type": "string" }, { "type": "number" }]
        }
    },
    "required": ["systemName", "actionName", "message"],
    "unevaluatedProperties": false
}
