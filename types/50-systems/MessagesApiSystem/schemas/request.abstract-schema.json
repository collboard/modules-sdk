{
    "$comment": "This is base abstract schema for all message API requsts",
    "$id": "https://schema.collboard.com/request",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Abstract message API request",
    "type": "object",
    "properties": {
        "type": {
            "description": "Type of the message.\nIn this case \"REQUEST\", in other cases for example \"RESPONSE\" or \"EVENT\"",
            "const": "REQUEST"
        },
        "requestId": {
            "description": "Custom arbitrarily ID of request which will be used to identify it in the response",
            "oneOf": [{ "type": "string" }, { "type": "number" }]
        }
    },
    "required": ["type"]
}
